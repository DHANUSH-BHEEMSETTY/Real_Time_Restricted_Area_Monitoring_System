# Cloud requirements — FastAPI dashboard only (no torch/cv2/streamlit)
# Loose pins so pip resolves compatible wheels for the pinned Python version.

fastapi>=0.115.0
uvicorn>=0.34.0
starlette>=0.45.0
jinja2>=3.1.0
pydantic>=2.10.0
supabase>=2.10.0
pandas>=2.2.0
python-multipart>=0.0.9
